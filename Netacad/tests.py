numbers = [10,5,6,7,8]
numbers[0] = 111 # replacing the first element
numbers[1] = numbers[4]# replacing the second element with the fifth
# la funcion len() devuelve la longitud de una lista
del numbers[1]
print(numbers, len(numbers))
print(numbers[-1]) # last element
